using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * @author Kyle Macdonald
 * @date 4/1/2024
 * Prerequisite - a snowmobile object with the snowmobile moving script attached
 * Output - Script should emit a raycast down to keep the snowmobile on the ground when going uphill and downhill
 */

public class FlattenSnowmobile : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
